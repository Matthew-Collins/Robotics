' Gambas class file

Sub SetWarnings(Value As Boolean)
End

Public Enum Board, Other
Public Enum {In}, Out

Sub SetMode(Mode As Integer)
End

Sub SetUp(Pin As Integer, Direction As Integer)
End Sub

Function PWM(Pin As Integer, Wave As Integer) As PWM
  Dim p As New PWM
  p.Pin = Pin
  p.Wave = Wave
  Return p
End

Sub CleanUp()
End

